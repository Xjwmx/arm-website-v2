---
const { path } = Astro.props;

import { Image } from 'astro:assets';
const imageFiles = await Astro.glob(path);

console.log( path, imageFiles)
---



<sl-carousel pagination navigation mouse-dragging loop>
{
imageFiles.map((img) => (
	<sl-carousel-item>
	  <Image
        src={img.default}
        width="600px"
        alt="Photo of a pizza"
      />
	</sl-carousel-item>
))}

</sl-carousel>

	<sl-carousel-item>
	  <img
		alt="The sun shines on the mountains and trees (by Adam Kool on Unsplash)"
		src="/assets/examples/carousel/mountains.jpg"
	  />
	</sl-carousel-item>
  </sl-carousel>
  